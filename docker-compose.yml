
services:
  app:
    build: .
    ports:
      - "8080:8080"

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: rahul
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: employeeManagement
    ports:
      - 5433:5432


